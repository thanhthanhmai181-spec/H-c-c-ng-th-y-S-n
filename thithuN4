<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JLPT N4 - SONKURO</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700;900&family=Poppins:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- Font Awesome 6 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --sakura: #ffb7c5;
            --sakura-dark: #e892a5;
            --indigo: #1a1a2e;
            --indigo-light: #16213e;
            --crimson: #c8102e;
            --crimson-glow: #e63946;
            --gold: #d4a853;
            --gold-light: #f0d78c;
            --white: #fafafa;
            --paper: #fdf6f0;
            --text: #2c2c2c;
            --text-light: #5a5a5a;
            --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
            --shadow-md: 0 8px 32px rgba(0, 0, 0, 0.12);
            --shadow-lg: 0 20px 60px rgba(0, 0, 0, 0.2);
            --shadow-glow: 0 0 40px rgba(200, 16, 46, 0.3);
            --radius-sm: 12px;
            --radius-md: 20px;
            --radius-lg: 28px;
            --transition: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Poppins', 'Noto Sans JP', sans-serif;
            background: #0f0c1a;
            background-image:
                radial-gradient(ellipse at 15% 20%, rgba(200, 16, 46, 0.08) 0%, transparent 55%),
                radial-gradient(ellipse at 85% 75%, rgba(212, 168, 83, 0.07) 0%, transparent 55%),
                radial-gradient(ellipse at 50% 50%, rgba(26, 26, 46, 1) 0%, #0a0812 100%);
            min-height: 100vh;
            color: var(--text);
            overflow-x: hidden;
            position: relative;
        }

        /* ============ PARTICLES BACKGROUND ============ */
        .particles-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }
        .particle {
            position: absolute;
            background: var(--sakura);
            border-radius: 50%;
            opacity: 0;
            animation: floatUp 8s infinite ease-in;
            pointer-events: none;
        }
        .particle:nth-child(odd) {
            background: var(--sakura-dark);
        }
        .particle:nth-child(3n) {
            background: var(--gold-light);
            animation-duration: 10s;
        }
        .particle:nth-child(5n+2) {
            background: #fff;
            animation-duration: 7s;
        }
        @keyframes floatUp {
            0% {
                transform: translateY(105vh) translateX(0) scale(0);
                opacity: 0;
            }
            10% {
                opacity: 0.7;
                scale: 1;
            }
            30% {
                transform: translateY(70vh) translateX(30px) scale(1.2);
                opacity: 0.6;
            }
            60% {
                transform: translateY(35vh) translateX(-25px) scale(0.8);
                opacity: 0.35;
            }
            90% {
                transform: translateY(5vh) translateX(15px) scale(0.3);
                opacity: 0.08;
            }
            100% {
                transform: translateY(-10vh) translateX(0) scale(0);
                opacity: 0;
            }
        }

        /* ============ MAIN CONTAINER ============ */
        .main-wrapper {
            position: relative;
            z-index: 1;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px 24px 60px;
        }

        /* ============ HEADER ============ */
        .header {
            text-align: center;
            padding: 50px 20px 30px;
            position: relative;
        }
        .header-badge {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(255, 255, 255, 0.04);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 50px;
            padding: 8px 22px;
            margin-bottom: 18px;
            animation: fadeInDown 0.8s ease-out;
        }
        .header-badge .dot {
            width: 10px;
            height: 10px;
            background: var(--crimson-glow);
            border-radius: 50%;
            animation: pulse-dot 1.8s infinite;
        }
        .header-badge span {
            font-size: 0.9rem;
            color: #ccc;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-weight: 500;
        }
        @keyframes pulse-dot {
            0%,
            100% {
                box-shadow: 0 0 0 0 rgba(230, 57, 70, 0.7);
            }
            50% {
                box-shadow: 0 0 0 14px rgba(230, 57, 70, 0);
            }
        }
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .header .title-jp {
            font-family: 'Noto Sans JP', sans-serif;
            font-size: 3.2rem;
            font-weight: 900;
            background: linear-gradient(135deg, #ffffff 0%, #e0c9a6 40%, #d4a853 70%, #f0d78c 100%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 6px;
            margin-bottom: 4px;
            animation: fadeInDown 0.8s ease-out 0.1s both;
            filter: drop-shadow(0 0 18px rgba(212, 168, 83, 0.25));
        }
        .header .title-vn {
            font-size: 1.35rem;
            font-weight: 600;
            color: #e8e8e8;
            letter-spacing: 3px;
            animation: fadeInDown 0.8s ease-out 0.2s both;
        }
        .header .subtitle {
            margin-top: 10px;
            font-size: 0.95rem;
            color: #999;
            letter-spacing: 1px;
            animation: fadeInDown 0.8s ease-out 0.3s both;
        }
        .header-divider {
            width: 80px;
            height: 3px;
            background: var(--crimson-glow);
            margin: 18px auto 0;
            border-radius: 2px;
            animation: fadeInDown 0.8s ease-out 0.35s both;
        }
        /* Sakura decorative line */
        .sakura-line {
            display: flex;
            justify-content: center;
            gap: 16px;
            margin-top: 14px;
            animation: fadeInDown 0.8s ease-out 0.4s both;
        }
        .sakura-line i {
            color: var(--sakura);
            font-size: 1.1rem;
            opacity: 0.7;
            animation: sway 3s infinite ease-in-out;
        }
        .sakura-line i:nth-child(2) {
            animation-delay: 0.4s;
        }
        .sakura-line i:nth-child(3) {
            animation-delay: 0.8s;
        }
        .sakura-line i:nth-child(4) {
            animation-delay: 1.2s;
        }
        .sakura-line i:nth-child(5) {
            animation-delay: 1.6s;
        }
        @keyframes sway {
            0%,
            100% {
                transform: rotate(0deg) translateY(0);
            }
            25% {
                transform: rotate(6deg) translateY(-3px);
            }
            75% {
                transform: rotate(-6deg) translateY(-2px);
            }
        }

        /* ============ EXAM CARDS GRID ============ */
        .cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 28px;
            margin-top: 30px;
            padding: 0 10px;
        }
        @media (min-width: 1200px) {
            .cards-grid {
                grid-template-columns: repeat(5, 1fr);
            }
        }
        @media (max-width: 1199px) and (min-width: 768px) {
            .cards-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }
        @media (max-width: 767px) {
            .cards-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 16px;
            }
            .header .title-jp {
                font-size: 2.2rem;
                letter-spacing: 3px;
            }
            .header .title-vn {
                font-size: 1.1rem;
            }
        }
        @media (max-width: 480px) {
            .cards-grid {
                grid-template-columns: 1fr 1fr;
                gap: 12px;
            }
        }

        /* ============ CARD STYLE ============ */
        .exam-card {
            position: relative;
            background: rgba(255, 255, 255, 0.04);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--radius-md);
            padding: 32px 24px 28px;
            text-align: center;
            cursor: pointer;
            transition: var(--transition);
            overflow: hidden;
            animation: cardAppear 0.7s ease-out both;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 14px;
            text-decoration: none;
            color: inherit;
        }
        .exam-card:nth-child(1) {
            animation-delay: 0.15s;
        }
        .exam-card:nth-child(2) {
            animation-delay: 0.25s;
        }
        .exam-card:nth-child(3) {
            animation-delay: 0.35s;
        }
        .exam-card:nth-child(4) {
            animation-delay: 0.45s;
        }
        .exam-card:nth-child(5) {
            animation-delay: 0.55s;
        }
        @keyframes cardAppear {
            from {
                opacity: 0;
                transform: translateY(50px) scale(0.9);
            }
            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        .exam-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle at 50% 50%, rgba(200, 16, 46, 0.12) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.5s ease;
            pointer-events: none;
        }
        .exam-card:hover::before {
            opacity: 1;
        }
        .exam-card:hover {
            transform: translateY(-10px);
            border-color: rgba(200, 16, 46, 0.5);
            box-shadow: var(--shadow-lg), 0 0 50px rgba(200, 16, 46, 0.18);
            background: rgba(255, 255, 255, 0.07);
        }
        .exam-card:active {
            transform: translateY(-4px) scale(0.97);
            transition: 0.1s ease;
        }

        /* Card icon circle */
        .card-icon-wrap {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: rgba(200, 16, 46, 0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: var(--crimson-glow);
            transition: var(--transition);
            position: relative;
            z-index: 1;
        }
        .exam-card:hover .card-icon-wrap {
            background: rgba(200, 16, 46, 0.28);
            box-shadow: 0 0 30px rgba(200, 16, 46, 0.35);
            color: #ff4d5a;
        }
        .card-icon-wrap i {
            transition: var(--transition);
        }
        .exam-card:hover .card-icon-wrap i {
            transform: scale(1.15) rotate(-5deg);
        }

        .card-year {
            font-family: 'Noto Sans JP', 'Poppins', sans-serif;
            font-size: 1.5rem;
            font-weight: 800;
            color: #fff;
            letter-spacing: 2px;
            position: relative;
            z-index: 1;
        }
        .card-label {
            font-size: 0.85rem;
            color: #aaa;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            position: relative;
            z-index: 1;
        }
        .card-arrow {
            margin-top: 6px;
            font-size: 1.2rem;
            color: var(--crimson-glow);
            opacity: 0.6;
            transition: var(--transition);
            position: relative;
            z-index: 1;
        }
        .exam-card:hover .card-arrow {
            opacity: 1;
            transform: translateX(6px);
        }

        /* Card accent line */
        .card-accent {
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 50px;
            height: 3px;
            background: var(--crimson-glow);
            border-radius: 3px 3px 0 0;
            transition: var(--transition);
        }
        .exam-card:hover .card-accent {
            width: 100px;
            background: #ff4d5a;
        }

        /* ============ FOOTER ============ */
        .footer-note {
            text-align: center;
            margin-top: 50px;
            color: #555;
            font-size: 0.85rem;
            letter-spacing: 1px;
            animation: fadeInDown 0.8s ease-out 0.6s both;
        }
        .footer-note i {
            color: var(--sakura);
            margin: 0 4px;
        }

        /* ============ POPUP NOTICE (optional) ============ */
        .popup-hint {
            display: inline-block;
            margin-top: 8px;
            font-size: 0.8rem;
            color: #f0d78c;
            letter-spacing: 1px;
            opacity: 0.85;
        }
    </style>
</head>
<body>

    <!-- Sakura particles background -->
    <div class="particles-container" id="particlesContainer"></div>

    <!-- Main Content -->
    <div class="main-wrapper">
        <!-- Header -->
        <header class="header">
            <div class="header-badge">
                <span class="dot"></span>
                <span>SONKURO - Luyện thi JLPT</span>
            </div>
            <h1 class="title-jp">日本語能力試験 N4</h1>
            <p class="title-vn">THI THỬ JLPT N4</p>
            <p class="subtitle">Luyện tập với 5 bộ đề sát thực tế</p>
            <div class="header-divider"></div>
            <div class="sakura-line">
                <i class="fa-solid fa-fan"></i>
                <i class="fa-solid fa-fan"></i>
                <i class="fa-solid fa-fan"></i>
                <i class="fa-solid fa-fan"></i>
                <i class="fa-solid fa-fan"></i>
            </div>
        </header>

        <!-- Exam Cards Grid -->
        <div class="cards-grid" id="cardsGrid">
            <!-- Đề 1: JLPT N4 - 7/2024 -->
            <div class="exam-card" data-url="https://sites.google.com/view/lopthayson/n4/thi-th%E1%BB%AD/jlpt-n4-72024" data-title="JLPT N4 - Tháng 7/2024">
                <div class="card-icon-wrap">
                    <i class="fa-solid fa-scroll"></i>
                </div>
                <div class="card-year">2024</div>
                <div class="card-label">Đề thi tháng 7</div>
                <div class="card-arrow"><i class="fa-solid fa-arrow-right"></i></div>
                <div class="card-accent"></div>
            </div>

            <!-- Đề 2: JLPT N4 - 12/2021 -->
            <div class="exam-card" data-url="https://sites.google.com/view/lopthayson/n4/thi-th%E1%BB%AD/jlpt-n4-122021" data-title="JLPT N4 - Tháng 12/2021">
                <div class="card-icon-wrap">
                    <i class="fa-solid fa-book-open"></i>
                </div>
                <div class="card-year">2021</div>
                <div class="card-label">Đề thi tháng 12</div>
                <div class="card-arrow"><i class="fa-solid fa-arrow-right"></i></div>
                <div class="card-accent"></div>
            </div>

            <!-- Đề 3: JLPT N4 - 2018 -->
            <div class="exam-card" data-url="https://sites.google.com/view/lopthayson/n4/thi-th%E1%BB%AD/jlpt-n4-2018" data-title="JLPT N4 - Năm 2018">
                <div class="card-icon-wrap">
                    <i class="fa-solid fa-pen-fancy"></i>
                </div>
                <div class="card-year">2018</div>
                <div class="card-label">Đề thi chính thức</div>
                <div class="card-arrow"><i class="fa-solid fa-arrow-right"></i></div>
                <div class="card-accent"></div>
            </div>

            <!-- Đề 4: JLPT N4 - 2017 -->
            <div class="exam-card" data-url="https://sites.google.com/view/lopthayson/n4/thi-th%E1%BB%AD/jlpt-n4-2017" data-title="JLPT N4 - Năm 2017">
                <div class="card-icon-wrap">
                    <i class="fa-solid fa-feather-pointed"></i>
                </div>
                <div class="card-year">2017</div>
                <div class="card-label">Đề thi chính thức</div>
                <div class="card-arrow"><i class="fa-solid fa-arrow-right"></i></div>
                <div class="card-accent"></div>
            </div>

            <!-- Đề 5: JLPT N4 - 2014 -->
            <div class="exam-card" data-url="https://sites.google.com/view/lopthayson/n4/thi-th%E1%BB%AD/jlpt-n4-2014" data-title="JLPT N4 - Năm 2014">
                <div class="card-icon-wrap">
                    <i class="fa-solid fa-torii-gate"></i>
                </div>
                <div class="card-year">2014</div>
                <div class="card-label">Đề thi chính thức</div>
                <div class="card-arrow"><i class="fa-solid fa-arrow-right"></i></div>
                <div class="card-accent"></div>
            </div>
        </div>

        <!-- Footer note -->
        <p class="footer-note">
            <i class="fa-solid fa-circle-info"></i> Nhấp vào bất kỳ thẻ đề thi nào — một cửa sổ làm bài riêng sẽ mở ra ngay lập tức.
            <br><i class="fa-solid fa-heart" style="color: var(--crimson-glow);"></i> Chúc bạn ôn luyện hiệu quả! がんばってください！
            <span class="popup-hint"><i class="fa-solid fa-up-right-from-square"></i> (Cửa sổ popup sẽ được mở)</span>
        </p>
    </div>

    <script>
        (function() {
            // ============ GENERATE SAKURA PARTICLES ============
            const particlesContainer = document.getElementById('particlesContainer');
            function createParticles() {
                const count = 35;
                for (let i = 0; i < count; i++) {
                    const particle = document.createElement('div');
                    particle.classList.add('particle');
                    const size = Math.random() * 8 + 3;
                    particle.style.width = size + 'px';
                    particle.style.height = size + 'px';
                    particle.style.left = Math.random() * 100 + '%';
                    particle.style.animationDelay = Math.random() * 10 + 's';
                    particle.style.animationDuration = (Math.random() * 8 + 6) + 's';
                    particlesContainer.appendChild(particle);
                }
            }
            createParticles();

            // ============ OPEN EXAM IN POPUP WINDOW ============
            const cardsGrid = document.getElementById('cardsGrid');

            function openExamWindow(url, title) {
                // Kích thước tối ưu để làm bài: rộng 1100, cao 800, có thanh công cụ và thanh cuộn
                const features = [
                    'width=1100',
                    'height=800',
                    'left=' + Math.max(0, (window.screen.width - 1100) / 2),
                    'top=' + Math.max(0, (window.screen.height - 800) / 2),
                    'resizable=yes',
                    'scrollbars=yes',
                    'toolbar=yes',
                    'location=yes',
                    'menubar=no',
                    'status=yes'
                ].join(',');

                const examWindow = window.open(url, title || 'JLPT_N4_Exam', features, 'noopener,noreferrer');

                // Nếu trình duyệt chặn popup, thông báo nhẹ nhàng
                if (!examWindow || examWindow.closed || typeof examWindow.closed === 'undefined') {
                    alert('⚠️ Trình duyệt đã chặn cửa sổ popup. Vui lòng cho phép popup cho trang này hoặc nhấn giữ Ctrl khi nhấp vào đề thi để mở trong tab mới.');
                }
            }

            // Sự kiện click cho các thẻ (event delegation)
            cardsGrid.addEventListener('click', function(e) {
                const card = e.target.closest('.exam-card');
                if (!card) return;
                const url = card.getAttribute('data-url');
                const title = card.getAttribute('data-title') || 'JLPT N4 - Đề thi';
                if (url) {
                    openExamWindow(url, title);
                }
            });

            // Thêm hiệu ứng ripple khi click (giữ nguyên từ phiên bản trước)
            document.querySelectorAll('.exam-card').forEach(card => {
                card.addEventListener('mousedown', function(e) {
                    const ripple = document.createElement('span');
                    const rect = card.getBoundingClientRect();
                    const size = Math.max(rect.width, rect.height);
                    ripple.style.cssText = `
                        position: absolute;
                        border-radius: 50%;
                        background: rgba(255,255,255,0.2);
                        width: ${size}px;
                        height: ${size}px;
                        left: ${e.clientX - rect.left - size/2}px;
                        top: ${e.clientY - rect.top - size/2}px;
                        transform: scale(0);
                        animation: rippleEffect 0.6s ease-out forwards;
                        pointer-events: none;
                        z-index: 0;
                    `;
                    card.appendChild(ripple);
                    ripple.addEventListener('animationend', () => ripple.remove());
                });
            });

            // Thêm keyframe cho ripple
            const rippleStyle = document.createElement('style');
            rippleStyle.textContent = `
                @keyframes rippleEffect {
                    to { transform: scale(1.5); opacity: 0; }
                }
            `;
            document.head.appendChild(rippleStyle);

            console.log('🌸 JLPT N4 - SONKURO: Sẵn sàng! Nhấn vào thẻ để mở cửa sổ làm bài.');
            console.log('📚 5 đề thi JLPT N4 đã được liên kết trực tiếp.');
        })();
    </script>
</body>
</html>
